# Model Artifacts TODO

Use this directory to document or organize trained model outputs.

- Keep source code for neural networks under `beergame.policy`.
- Save generated checkpoints outside the package by default, for example `models/`.
- Record model naming conventions for baseline, improved, and multi-agent runs.
- Add lightweight model metadata files when experiments are implemented.
